<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zero.healthmonitoring">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <application
        android:name=".SuperApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <service
            android:name="com.bde.parentcyTransport.ACSUtilityService"
            android:enabled="true" />

        <activity
            android:name=".presenter.MainActivity">

        </activity>
        <activity android:name=".presenter.SpoPresenter" />
        <activity android:name=".presenter.SpoJavaPresenter" />
        <activity android:name=".presenter.RecordPresenter" />
        <activity android:name=".presenter.DemoMainActivity" />
        <activity
            android:name=".presenter.LoginPresenter"
            android:configChanges ="orientation|screenSize"
            android:label="@string/title_activity_login_presenter">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presenter.RegisterPresenter"/>
    </application>

</manifest>